<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:layout_margin="16dp"
              android:gravity="center_vertical">

    <ImageView
        android:id="@+id/trailer_play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_play_arrow_black_48dp"/>

    <ImageView
        android:id="@+id/trailer_share"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_share_black_48dp"/>

    <TextView
        android:id="@+id/trailer_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/trailer_share"
        android:layout_toStartOf="@id/trailer_share"
        android:layout_toRightOf="@id/trailer_play"
        android:layout_toEndOf="@id/trailer_play"
        android:textAppearance="?android:attr/textAppearanceMedium"
        tools:text="Trailer 1"/>

</RelativeLayout>